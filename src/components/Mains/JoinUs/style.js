import styled from "styled-components";

const JoinUsparagrf = styled.p`
  color: #96bb7c;
`;

const JoinUsh1 = styled.p`
  color: white;
`;

const JoinInput = styled.input``;
const JoinBtn = styled.button``;

export { JoinUsparagrf, JoinUsh1, JoinInput, JoinBtn };
